# AGENTS.md

- When editing JSON or other text config files in this workspace, preserve the original encoding and use UTF-8 without BOM.
- Do not rewrite a file through a tool or editor setting that adds BOM or changes line endings unless the user explicitly asks for it.
- Prefer minimal, targeted edits so unchanged content keeps its existing byte representation.
